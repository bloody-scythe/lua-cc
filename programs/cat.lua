---FIXME everything
function fileExists(file)
    if fs.isDir == false and fs.exists(file)
        then return true
        else return false
    end
end
    
function main()

for k,arg in pairs(arg) do
    if k ~= 0 then
        print(arg)
    end
end

end

main()
fs.isDis
